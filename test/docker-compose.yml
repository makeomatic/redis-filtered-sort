redis:
  build: ../
  hostname: redis
  expose:
    - "6379"
tester:
  image: makeomatic/node:12
  links:
    - redis
  working_dir: /src
  volumes:
    - ${PWD}:/src
  environment:
    NODE_ENV: "docker"
    DEBUG: ${DEBUG}
  command: 'tail -f /dev/null'
