redis:
  image: tenstartups/alpine-redis
  container_name: redis
  hostname: redis
  expose:
    - "6379"

tester:
  image: ${IMAGE}
  links:
    - redis
  working_dir: /src
  volumes:
    - ${PWD}:/src
  environment:
    NODE_ENV: "docker"
  command: 'true'
